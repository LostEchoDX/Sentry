@page
@model SentryAPI.Pages.PoIs.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PoI[0].drone_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PoI[0]._class)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PoI[0].f_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PoI[0].picture)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PoI[0].latitude)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PoI[0].longitude)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PoI) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.drone_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item._class)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.f_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.picture)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.latitude)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.longitude)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
